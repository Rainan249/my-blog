---
id: 1781347139623
slug: os期末复习
title: os期末复习
category: 未分类
date: 2026-06-13
readTime: 56 分钟
excerpt:   第1章  基本概述     1.1.1+1.1.2    基本概述     (1)概念 操作系统是控制和管理整个计算机系统的硬件和软件资源 并合理的组织调度计算机的工作和资源的分配 提供给用户和其他软件方便的接口和环境 是计算机系统中最...
---

# 第1章  基本概述

## 1.1.1+1.1.2    基本概述
### (1)概念
操作系统是控制和管理整个计算机系统的硬件和软件资源 并合理的组织调度计算机的工作和资源的分配 提供给用户和其他软件方便的接口和环境 是计算机系统中最基本的系统软件
### (2)特性
 ①操作系统是系统资源的管理者
 - 提供的功能
	- 处理器管理
	- 存储器管理
	- 文件管理
	- 设备管理
- 目标：安全 高效
 ②如图 向上提供方便易用的服务
 ③操作系统是最接近硬件的一层软件
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/648d814a2b66eabe2ac672a7f04465e7.png)
### (3)操作系统的方式
- GUI(Graphical User Interface) 图形化用户接口
- 连接命令接口---终端的形式 输入一句 执行一句(这是交互式的操作)
- 脱机命令接口---是一个`*.bat`文件  批处理的方式
- 程序接口---可以在程序中进行系统调用来使用程序接口 只能通过程序代码间接使用
## 1.1.3  四个基本特征
### (1)并发
指的是两个或多个事件在同一时间间隔内发生，这些事情宏观上是同时发生的但是在微观上是交替发生的
- 并行：微观上 两个或多个事件在同一时刻同时发生
- 单核CPU同一时刻只能执行一个程序 各个程序只能并发执行
- 多核CPU同一时刻可以执行多个程序 多个程序可以并行地执行
### (2)共享
- 互斥共享方式：系统中的某些资源 虽然可提供给多个进程使用 但一个时间段只允许一个进程访问
	- 比如摄像头、键盘、鼠标
- 同时共享方式：系统中的某些资源 允许一个时间段"同时"提供给多个进程使用
	- 比如多个进程读取同一份文件
	- "同时"往往是宏观上的  但是微观上进程可能是交替的对资源进行访问(分时共享)
### (3)虚拟
- 空分复用技术(如虚拟存储器技术)
- 时分复用技术(如虚拟处理器)
### (4)异步 
- 指的是在多道程序环境下 允许多个程序并发执行 但是由于资源有限 进程的执行是断断续续的
## 1.2 计算机发展和分类
### (1)批处理阶段--多道批处理系统
- 优点：多道程序并发执行 共享计算机资源  资源利用率大幅提升
- 缺点：用户响应时间长 没有人机交互功能
### (2)分时操作系统
- 计算机以时间片为单位轮流为各个用户/作业服务，各个用户可以通过终端和计算机进行交互
- 优点：用户请求可以被及时响应 解决了交互问题 允许多个用户使用一台计算机
- 缺点：不能处理一些紧急任务
### (3)实时操作系统
- 优点：能够优先响应一些紧急任务 某些紧急任务不需要时间片排队
- 分类：
	- 硬实时：必须在绝对严格的规定时间内完成处理
	- 软实时：能接受偶尔违反时间规定
## 1.3.1 操作系统运行机制
- 编译：c语言代码压过编译器"翻译"变为二进制的机器指令
- 状态：
	- 内核态(核心态、管态)：运行的是内核程序 可以执行特区指令
	- 用户态(目态)：运行的是应用程序 只能执行非特权指令
		- 内核态--->用户态：执行特权指令修改PSW(程序状态字寄存器)标志位为用户态 意味着操作系统主动让出CPU使用权
		- 用户态--->内核态：由“中断”引发 硬件自动完成变态过程 触发中断意味着操作系统将强行夺回CPU的使用权
			- 除了非法使用特权指令之外 还有很多事件会触发中断信号
			- 一个共性是，需要操作系统介入的地方都会触发中断信号
## 1.3.2 中断和异常
①内中断(也称为异常)--中断信号来自CPU的内部
- 在应用程序执行特权指令
- 执行除法指令发现除数为0
- 陷入指令：应用程序想请求操作系统内核的服务(比如系统调用)
②外中断(也称为中断)--中断信号来自CPU的外部
- 时钟中断--由时钟部件发来的中断信号
- 输入输出设备任务完成发出了中断请求
## 1.3.3  系统调用
- 是操作系统提供给应用程序使用的接口 可以 理解为一种可供应用程序调用的特殊函数
- 库函数：
	- 不涉及系统调用：比如取绝对值的函数
	- 涉及系统调用：比如创建一个新文件
- 对系统资源有关的操作一定是系统调用
- 过程：
	- 传递系统调用参数-->执行陷入指令(用户态)-->执行相应的内请求核程序处理系统调用(核心态)-->返回应用程序

## 1.4 操作系统的体系结构
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/e79a4b27b36c3a9e798498a4b339796e.png)
- Ubuntu、centos主要是实现非内核的功能 而内核都是用Linux内核
- 操作系统的内核
	- 时钟管理：实现计时的功能
	- 中断处理：负责实现中断机制
	- 原语：
		- 是一种特殊的程序
		- 处于操作系统最底层 是最接近硬件的部分
		- 这种程序的运行具有原子性--只能一次性完成
		- 运行时间较短、调用频繁
	- 对系统资源进行管理的功能
		- 进程管理
		- 存储器管理
		- 设备管理
### (1)大内核  --cpu进行两次的状态转换
包含时钟管理内核等和资源管理功能
### (2)微内核  --cpu进行六次的状态转换
只包含时钟管理等
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/f0fb52aec2636e649faa0c69a6d72aa6.png)
### (3)分层结构
内核分多层 类似于一个洋葱圈 每层可以调用更低一层提供的接口
### (4)模块化
- 先将操作系统分为进程管理、存储器管理、文件管理....
- 再将这些模块细分为子模块
- 内核=主模块+可加载模块
- 主模块：只负责核心功能 如进程调度、内存管理等
- 可加载内核模块：可以动态加载新模块到内核，而无需重新编译整个内核
### (5)外核
视频 1.4  14：00

![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/009658fbf9a0470617fc391c7c5a6b93.png)
## 1.5 操作系统的引导
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/787286585851b6d4a8f4b0ae8d6fd851.png)
### c盘结构
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/6b7d722ee74b93e9a9ea108fd2ae5c23.png)
### 启动程序
①CPU从一个特定的主存地址开始 取指令 执行ROM中的引导程序(先进行硬件自检再开机)
②将磁盘的第一块--主引导记录读入内存，执行磁盘引导程序，扫描分区表
③从活动分区(又称主分区，即安装了操作系统的分区)读入分区引导记录，执行其中程序
④从根目录下找到完整的操作系统初始化程序(即启动管理器)并执行，完成“开机”的一系列操作 

## 1.6  虚拟机
使用虚拟化的技术 将一台物理机器虚拟化为多台虚拟机器 每个虚拟机器可以独立运行一个操作系统
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/888f387b347aa3eb9b565062faf8b2f2.png)    ![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/296f6b50e87331dc24661d7ee8933109.png)
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/24f71775db3610b0fafa39f78bf8b4b7.png)


# 第2章  进程和线程
## 2.1.1+2.1.2 进程的概念
### (1)程序
是静态的 是存放在磁盘的可执行文件 就是一系列的指令集合
### (2)进程
- 动态的，是程序的一次执行过程
- 组成
	- PCB
	- 程序段
	- 数据段
### (3)PCB
#### ①说明
进程的组成 PCB  (Process Control Block)（进程控制块）
是进程存在的唯一标识，当进程被创建时，操作系统为其创建PCB，结束时会回收
#### ②组成
- 进程描述信息
	- PID :进程的编号 (具有唯一性不重复性)
	- UID：进程所属用户ID
- 进程控制和管理信息
	- CPU、磁盘、网络流量使用情况....
	- 进程当前状态：就绪态、阻塞态、运行态
- 资源分配清单
	- 正在使用哪些文件
	- 正在使用哪些内存区域
	- 正在使用哪些I/O设备
- 处理机相关信息
	- 如PSW、PC等各种寄存器的值
### (4)程序段和数据段
- 程序文件编译后变成可执行文件
- 可执行文件运行前装入内存 细分为
	- 程序段：包含给CPU执行的程序指令
	- 数据段：包含程序运行过程中产生的各种数据
### (5)特征
#### ①动态性
进程是程序的一次执行过程，是动态产生、变化和消亡的
#### ②并发性
内存中多个进程实体，各进程可并发的执行
#### ③独立性
进程是能独立运行、独立获得资源、独立接受调度的基本单位
#### ④异步性
各进程按各自独立的、不可预知的速度向前推进，操作系统要提供“进程同步机制”来解决异步问题
#### ⑤结构性
每个进程都会配置一个PCB，结构上看，进程由程序段、数据段、PCB组成
## 2.1.3 进程的状态与转换
### (1)五种状态
#### ①三种基本状态
- 运行态 (Running)
	- 占有CPU 并在CPU上运行
	- 单个CPU同一时刻只会有一个进程处于运行态 多核CPU可能有多个进程处于运行态
- 就绪态(Ready)
	- 已经具备运行条件 但由于没有空闲CPU 而暂时不能运行
- 阻塞态(Waiting/Blocked  等待态)
	- 因某件事而不能运行
#### ②另外两种状态
- 创建态(New  新建态)
	- 进程正在被创建 操作系统为进程分配资源 初始化PCB
- 终止态(Terminated  结束态)
	- 进程正在从系统中撤销 操作系统会回收进程拥有的资源 撤销PCB
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/27d2618faadf2094f1f11e692bdffa48.png)
### (2)进程的组织
#### ①链接方式
- 执行指针 ——> PCB2 ——> PCB3
- 就绪队列指针 ——> PCB5 ——> PCB1 ——>PCB4
- ......
#### ②索引方式
- 执行指针 ——> 一个索引表
- 就绪队列指针 ——> 一个索引表
- ....
## 2.1.4 进程的控制
### (1)原语
原语的执行具有原子性，即执行过程只能一气呵成，期间不允许被中断
### (2)进程的创建
- 创建原语
	- 申请空白PCB
	- 为新进程分配所需资源
	- 初始化PCB
	- 将PCB插入就绪队列
- 引起进程创建的事件
	- 用户登录
	- 作业调度
	- 提供服务
	- 应用请求
### (3)进程的终止
- 撤销原语
	- 从PCB集合中找到终止进程的PCB
	- 若进程正在运行 立即剥夺CPU 将CPU分配给其他进程
	- 终止其所有进程
	- 将该教程拥有的所有资源归还给父进程或操作系统
	- 删除PCB
- 引起进程终止的事件
	- 正常结束
	- 异常结束
	- 外接干预
### (4)阻塞和唤醒
- 进程的阻塞
	- 阻塞原语
		- 找到要阻塞的进程对应的PCB
		- 保护进程运行现场 将PCB状态信息设置为阻塞态 暂时停止进程运行
	- 引起进程阻塞的事件
		- 需要等待系统分配某种资源
		- 需要等待相互合作的其他进程完成工作
- 进程的唤醒
	- 唤醒原语
		- 在事件等待队列中找到PCB
		- 将PCB从等待序列移除 设置进程为就绪态
		- 将PCB插入就绪队列 等待被调度
	- 引起进程唤醒的事件
		- 等待的事件发生
### (5)切换
- 切换原语
	- 将运行环境信息存入到PCB
	- PCB移入相应队列
	- 选择另一个进程执行 并更新PCB
	- 根据PCB恢复新进程所需的运行环境
- 引起进程切换的事件
	- 当前进程时间片到
	- 有更高优先级的进程到达
	- 当前进程主动阻塞
	- 当前进程终止

## 2.1.5.1 进程通信
进程间通信(Inter-Process Communication,IPC) 是指两个进程间产生数据交互
- 共享存储
	- 原理
		- 进程在内存申请共享存储区
		- 通过增加“页表项”“段表项”可以将一片共享区域映射到各个进程的地址空间中
		- 需要保证各个进程对这个共享区域的访问是互斥的
	- 方式
		- 基于数据结构的共享
			- 比如共享空间只能放长度为10的数组
			- 特点：速度慢，限制多，低级的通信方式
		- 基于存储区的共享
			- 操作系统在内存中划出一块共享存储区，数据的形式、存放位置都由通信进程控制而不是操作系统
			- 特点：速度快，高级的通信方式
- 消息传递
	- 进程间的数据交换以格式化的消息为单位，进程通过操作系统提供的“发送消息/接受消息”两个原语进行数据交换
	- 包括：
		- 消息头：发生进程ID  接受进程ID  消息长度等格式化的信息
		- 消息体：传输给另一个进程的消息
	- 分类：
		- 直接通信方式
			- 进程P 发送原语，send(Q，msg)
			- 进程Q 接受原语，receive(P，&msg)
		- 间接通信方式
			- 进程P 发送原语，send(信箱A，msg)
			- 进程Q 接受原语，receive(信箱B，&msg)
- 管道通信
	- 管道是特殊的共享文件 又名为pipe文件 就是在内存中开辟一个固定大小的内存缓冲区 
	- 类似一个管道 两个进程通过管道相连
	- 特点：单向的 先进先出
		- 半双工通信：某一时间段只能实现单向的传输
		- 双向同时通信：需要设置两个管道
## 2.1.5.2 信号
- 信号量(Semaphore)：实现进程间的同步和互斥
- 信号(Signal)：实现进程间通信(IPC，Inter Process Communication
## 2.1.6 线程
>他说和进程一样 不讲
2.1.6.1 线程的概念
2.1.6.2 线程的实现方式和多线程模型
2.1.6.3 线程的状态与转换
## 2.2.1 调度的概念、层次
### (1)基本概念
当很多任务要处理 但是由于资源有限 这些事情没办法同时处理 这就需要某种规则来决定处理任务的先后顺序
### (2)三个层次
#### ①高级调度(作业调度)
- 作业：一个具体的任务
- 概念：
	- 按一定的原则从外存的作业后备队列中挑选一个作业调入内存，并创建进程
	- 每个作业只调入一次，调出一次
	- 作业调入时会建立PCB 调出才撤销PCB
- 简化理解：几个程序先启动哪一个
#### ②中级调度(内存调度)
- 按照某种策略决定将哪个处于挂起状态的进程同时调入内存
	- 内存不够时候可以将进程的数据调出外存 等内存空闲或者进程需要时再重新调入内存
	- 暂时调到外存的进程状态为挂起状态 被挂起的进程PCB会被组织成挂起队列
#### ③低级调度(进程调度/处理机调度)
- 按照某种策略从就绪队列中选取一个进程将处理机分配给它  
- 是操作系统中最基本的调度 频率很高
### (3)三层调度的联系、对比

|                | 要做什么                                   | 发生场景              | 发生频率 | 对进程的影响                   |
| -------------- | -------------------------------------- | ----------------- | ---- | ------------------------ |
| 高级调度<br>(作业调度) | 按照某种规则，从后备队列中选择合适的作业将其调入<br>内存，并为其创建进程 | 外存-->内存<br>(面向作业) | 最低   | 无-->创建态-->就绪态            |
| 中级调度<br>(内存调度) | 按照某种规则，从挂起队列中选择合适的进程将其数据<br>调回内存       | 外存-->内存<br>(面向进程) | 中等   | 挂起态-->就绪态<br>(阻塞挂起->阻塞态) |
| 低级调度<br>(进程调度) | 按照某种规则，从就绪队列中选择一个进程为其分配处<br>理机         | 内存-->CPU          | 最高   | 就绪态->运行态                 |

### (4)状态转换图
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/e6d76be1ee7f5e8d966ded0e033953d3.png)
## 2.2.2.1+2.2.4 进程调度时机、切换与过程、方式
- 进程调度(低级调度)：按照某种算法从就绪队列中选择一个进程为其分配处理机
- 需要进行进程调度与切换的情况
	- 运行的进程主动放弃处理机
		- 进程正常终止
		- 运行过程中发生异常而终止
		- 进程主动请求阻塞(如等待I/O)
	- 运行的进程被动放弃处理机
		- 进程的时间片用完
		- 有更紧急的事情要处理
		- 有优先级更高的基层进入就绪队列
	- 不能进行进程调度与切换的情况
		- 在处理中断的过程中
			- 中断处理过程复杂 与硬件密切相关 很难做到中断处理过程中进行进程切换
		- 进程在操作系统内核程序临界区中
			- 临界资源：一个时间段只允许一个进程使用的资源 各进程需要互斥的访问临界资源
			- 内核程序临界区：访问某种内核的数据结构
		- 在原子操作过程中
			- 原子操作不可中断 要一气呵成
- 进程调度的方式
	- 非剥夺调度方式(非抢占方式)
		- 只允许进程主动放弃处理机
		- 在运行过程中即使有更紧要的任务到达 也会执行当前的进程
		- 直到该进程终止或者主动要求进入阻塞态
	- 剥夺调度方式(抢占方式)
		- 在运行过程有更紧要的任务到达 则会立即停止正在执行的过程
		- 将处理器分为更紧要的任务
## 2.2.2.2 调度机和闲逛进程
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/3e899b580bff6cbd68d5348189c97bac.png)
② ③由调度程序引起 调度程序决定
- 触发”调度程序“的事件
	- 创建新进程
	- 进程退出
	- 运行进程阻塞
	- I/O中断发生
- 调度策略
	- 非抢占式调度策略，只有运行进程阻塞或退出才触发调度程序工作
	- 抢占式调度策略，每个时钟中断或k个时钟中断会触发调度程序工作
- 闲逛进程
	- 优先级最低
	- 可以是0指令地址 占一个完整的指令周期(指令周期末尾例行检查中断)
	- 能耗低
## 2.2.3 调度的目标
### (1)CPU利用率
利用率=忙碌的时间/总时间
### (2)系统吞吐量
单位时间内完成作业的数量
系统吞吐量=完成作业数量/所花时间
### (3)周转时间
#### ①周转时间、平均周转时间
周转时间=作业完成时间-作业提交时间
平均周转时间=各作业周转时间之和/作业数
#### ②带权周转时间、平均带权周转时间
带权周转时间=作业周转时间/作业实际运行的时间=(作业完成时间-作业提交时间)/作业实际运行的时间
### (4)等待时间
- 对于进程来说，等待时间就是指进程建立后等待被服务的时间之和，在等待I/O完成的期间其实进程也是在被服务的，所以不计入等待时间
- 对于作业来说，不仅要考虑建立进程后的等待时间，还要加上作业在外存后备队列中等待的时间
### (5)响应时间
指的是用户从提交请求到首次产生相应所用的时间
## 2.2.5 调度算法
- 饥饿：某进程/作业长期得不到服务
### (1)先来先服务FCFS
#### ①算法思想
从”公平“的角度考虑 先来先服务
#### ②算法规则
按照作业/进程的先后顺序进行服务
#### ③用于作业/进程调度
- 用于作业调度时，考虑的是哪个作业先到达后备队列
- 用于进程调度时，考虑的是哪个进程先到达就绪队列
#### ④是否可以抢占
非抢占式的算法
#### ⑤优缺点
- 优点
	- 公平、算法简单
- 缺点
	- 排在长作业后面的短作业需要等待很长的时间 带权周转时间很大
	- 即对长作业有利，对短作业不利
#### ⑥是否会导致饥饿
不会
### (2)短作业优先 SJF
题目 2.2.5.1   12：24
#### ①算法思想
追求最少的平均等待时间 最少的平均周转周期 最少的平均带权周转时间
#### ②算法规则
最短的作业/进程优先得到服务(所谓的最短，指的是要求服务时间最短)
- 抢占式：每次调度选择当前已经到达且运行时间最短的作业/进程
- 非抢占式：每当有新进程加入就绪队列改变时就需要调度 比较当前进程剩余时间和新进程所需的运行时间
#### ③用于作业/进程调度
即可用于作业调度也可以用于进程调度
用于进程调度时称为”短进程优先(SPF,Shortest Process First)算法“
#### ④是否可以抢占
- 默认是非抢占式的
SJF和SPF是非抢占式的算法 但是也有抢占式的--最短剩余时间算法(SRTN,Shortest Remaining Time Next)
#### ⑤优缺点
- 优点
	- ”最短的“平均等待时间、平均周转时间
- 缺点
	- 不公平 对短作业有利 对长作业不利 可能产生饥饿现象
	- 作业/进程的运行时间是由用户提供 并不一定真实 不一定能做到短作业优先
#### ⑥是否会导致饥饿
会 如果源源不断有短作业/进程到来 可能会使长作业/进程得不到服务 产生饥饿现象
### (3)高响应比优先 HRRN
#### ①算法思想
综合考虑作业/进程的等待时间和要求服务的时间
#### ②算法规则
在每次调度时先计算各个作业的响应比 选择响应比最高的作业/进程进行服务
非抢占式的调度算法 只有当前运行的进程主动放弃CPU时 (正常/异常完成，或主动阻塞)，才需要进行调度 调度时计算所有的就绪进程的响应比 选响应比最高的进程上处理机
响应比=(等待时间+要求服务时间)/要求服务时间
#### ③用于作业/进程调度
可以用于作业调度 也可以用于进程调度
#### ④是否可以抢占
非抢占式的算法 因此只有当前运行的作业/进程主动放弃处理机时 才需要调度 才需要计算响应比
#### ⑤优缺点
综合考虑了等待时间和运行时间(要求服务时间) 
等待时间相同时 要求服务时间短的优先(SJF的优点)
要求服务时间相同时 等待时间长的优先(FCFS的优点)
对于长作业来说 随着等待时间越来越久 其响应比也会越来越大 从而避免了作业饥饿的问题
#### ⑥是否会导致饥饿
不会
### (4)时间片轮转调度算法
#### ①算法思想
公平轮流的为各个进程服务 让每个进程在一定时间间隔内都可以得到响应
#### ②算法规则
按照各进程达到就绪队列的顺序 轮流让各个进程执行一个时间片 
若进程未在一个时间片内执行完 则剥夺处理机 将进程重新放到就地队列队尾重新排队
#### ③用于作业/进程调度
用于进程调度
#### ④是否可以抢占
若进程未在时间片内运行完 将被强行剥夺处理机使用权，因此时间片轮转调度算法属于抢占式的算法
由时钟装置发出时钟中断来通知CPU时间片已到
#### ⑤优缺点
- 优点：
	- 公平 相应快 适用于分时操作系统
- 缺点：
	- 由于高频率的进程切换 因此有一定的开销 不区任务的紧急程度
#### ⑥是否会导致饥饿
不会
### (5)优先级调度算法
题 2.2.5.2   20:30
#### ①算法思想
实时操作系统的出现 越来越多的应用场景需要根据任务的紧急程度来决定处理顺序
#### ②算法规则
每个作业有各自的优先级 调度时选择优先级最高的作业/进程
- 通常
	- 系统进程优先级高于用户进程
	- 前台进程优先级高于后台进程
	- 操作系统更偏好I/O型进程(或称为I/O繁忙型)   与其对应的是计算型进程(或称CPU繁忙型进程)
#### ③用于作业/进程调度
可以用与作业和进程调度 I/O调度也可以
#### ④是否可以抢占
有抢占式和非抢占式
- 非抢占式只需在进程主动放弃处理机时进行调度即可
- 抢占式还需在就绪队列变化识 检查是否会发生抢占
#### ⑤优缺点
- 优点：
	- 用优先级区分紧急程度、重要程度 适用于实时操作系统 可灵活的调整对各种作业/进程的偏好程度
- 缺点：
	- 若源源不断地有高优先级进程到来 则可能会导致饥饿
#### ⑥是否会导致饥饿
可能会
### (6)多级反馈队列调度算法
#### ①算法思想
对其他调度算法的折中权衡
#### ②算法规则
- 设置多级就绪队列 各级队列优先级从高到低 时间片从大到小
- 新进程到达时先进入第1级队列 按FCFS原则等待被分配时间片 
	- 若用完时间片进程还未结束 则进程进入下一级队列队尾
	- 如果此时已经是在最下级的队列 则重新放回该队列队尾
- 只有第k级队列为空时 才会为k+1级队头的进程分配时间片
#### ③用于作业/进程调度
用于进程调度
#### ④是否可以抢占
抢占式的算法 在k级队列的进程运行过程中 
若更上级的队列(1~k-1级)中进入了一个新进程
则由于新进程处于优先级更高的队列中 
因此 新进程会抢占处理机 原来运行的进程放回k级队列队尾
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/418be9f691e225ce858da7d7e39789db.png)

#### ⑤优缺点
- 对各类型进程相对公平(FCFS的优点)
- 每个新到达的进程都可以很快得到响应(RR的优点)
- 短进程只用较少的时间就可以完成(SPF的优点)
- 不必实现估计进程的运行时间
- 可灵活的调整对各类进程的偏好程度 比如CPU密集型进程、I/O密集型进程
#### ⑥是否会导致饥饿
否
### (7)多级队列调度算法
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/9185e804c176214fe9f850882e80140d.png)
- 队列之间可采取固定优先级或时间片划分
- 固定优先级：高优先级空时低优先级进程才能被调度
- 时间片划分：如三个队列分配时间50%、40%、10%
- 各个进程可采用不同的调度策略 比如
	- 系统进程采用优先级调度
	- 交互式队列采用RR
	- 批处理队列采用FCFS
## 2.2.6 多处理机调度
>2025考纲新增 暂时跳过
## 2.3.1 同步和互斥的概念
- 异步性：各并发执行的进程以各自独立的、不可预知的速度向前推进
- 同步性：亦称为直接制约关系，它是指为完成某种任务而建立的两个或多个进程，这些进程因为需要在某些位置上协调它们的工作次序而产生的制约关系。进程间的直接制约关系就是源于它们之间的相互合作
- 互斥性：间接制约关系。进程互斥指当一个进程访问某临界资源时，另一个想要访问该临界资源的进程必须等待。当前访问临界资源的进程访问结束，释放该资源之后，另一个进程才能去访问临界资源。
	- 临界资源：一个时间段内只允许一个进程使用的资源。比如许多物理设备(摄像头、打印机...)。此外还有许多变量、数据、内存缓冲区等都属于临界资源
对临界资源的互斥访问 在逻辑上可以分为以下四个部分
```cpp
do{
	entry section;        //进入区
	critical section;     //临界区
	exit section;         //退出区
	remainder section;    //剩余区
}
```
- 进入区：负责检查死后可以进入临界区，若可进入吗，则应设置正在访问临界资源的标志(上锁)，以阻止其他进程同时进入临界区
- 临界区：访问临界资源的那段代码
- 退出区：负责解除正在访问临界资源标志(解锁)
- 剩余区：做其他处理
互斥遵循的四个原则
- 空闲让忙：临界区空闲时，应允许一个进程访问
- 忙则等待：临界区正在被访问时，其他试图访问的进程需要等待
- 有限等待：要在有限时间内进入临界区，保证不会饥饿
- 让权等待：进不了临界区的进程，要释放处理机，防止忙等
## 2.3.2 进程互斥的实现方法
### (1)单标志算法
#### ①算法思想
两个进程在访问完临界区后会把使用临界区的权限转交给另一个进程。也就是说每个进程进入临界区的权限只能被另一个进程赋予
#### ②实现逻辑
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/f1742c12995c6f2dc1dc8b483cce9cee.png)
### (2)双标志检查法
#### ①算法思想
设置一个布尔型数组flag[] 数组中各个元素用来标记各进程想进入临界区的意愿
比如`flag[0]=true`表示0号进程现在想要进入临界区
每个进程在进入临界区之前先检查当前有没有背的进程想进入临界区
如果没有则把自身的标志`flag[0]`设为true 之后开始访问临界区
#### ②实现逻辑
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/e173cc5dc463590dc52efba85a258b96.png)
#### ③说明
违背了忙则等待问题 “检查”后，“上锁”前可能发生进程切换
### (3)双标志后检查
#### ①算法思想
大致相同 但是是先“上锁”后“检查”
#### ②实现逻辑
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/f7b145a3960584821aa319ffc98d7bb2.png)
#### ③说明
违背了空闲让进和有限等待的原则 可能会导致各进程都长期无法访问临界资源而产生“饥饿现象”
### (4)Peterson算法
#### ①算法思想
结合双标志法、单标志法的思想
如果双方都争着想进入临界区，那可以让进程尝试谦让过程
#### ②实现逻辑
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/b27a49f58a1bd17be0c7e581f0fb5878.png)
#### ③说明
解决了进程互斥问题，遵循了空闲让进、忙则等待、有限等待三个原则。但是依然未遵循让权等待原则
## 2.3.3 互斥锁
解决资源区最简单工具。一个进程在进入临界区时应获得锁 退出临界区时释放锁
函数acquire( )获得锁  函数release( )释放锁
如果锁是可用的 调用acquire( )会成功 且锁不再可用
当一个进程试图获取不可用的锁 会被阻塞 直到锁被释放
```cpp
acquire(){
	while(!available);  //忙等待
	available = false;  //获得锁
}
release(){
	available = true;   //释放锁
}
```
互斥锁的主要缺点是忙等待，当有一个进程在临界区中，任何其他进程在进入临界区时必须连续循环调用`acquire()`
当多个进程共享同一CPU时，就浪费了CPU周期
因此，互斥锁通常用于多处理器系统，一个线程可以在一个处理器上等待，不影响其他进程执行
特性
- 需忙等，进程时间片用完才下处理机，违反“让权等待”
- 优点：等待期间不用切换进程上下文，多处理器系统中，若上锁的时间短，则等待代价很低
- 常用于多处理器系统，一个核忙等，其他核照常工作，并快速释放临界区
## 2.3.4 信号量
### (1)信号量分类
- 用户进程可以通过使用操作系统提供的一对原语来对信号量进行操作，从而很方便的实现了进程互斥、进程同步
	- 信号量其实就是一个变量 可以用一个信号量表示系统中某种资源的梳理
	- 一对原语：`wait(S)`原语和`signal(S)`原语，可以把原语理解为自己写的函数
#### ①整型信号量
对信号量的操作只有三种即：初始化、P操作、V操作
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/7c1b4e8d96c4f92eaa400abbf9c7041e.png)
#### ②记录型信号量
用记录型数据结构表示的信号量
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/8194415798d6fd39e93cc9441641345b.png)
- 对信号量S的一次P操作意味着进程请求一个单位的该类资源，因此需要执行S.value-- 表示该资源数-1
	- 当S.value<0时表示该类资源已分配完毕，因此进程调用block原语进行自我阻塞(当前进程从从运行态->阻塞态)
	- 主动放弃处理机 并插入该类资源的等待S.L中
	- 该机制遵循了“让权等待”原则 不会出现“忙等现象”
- 对信号量S的一次V操作意味着进程释放一个单位的该类资源 因此需要执行S.value++ 表示依然有进程在等待该类资源
	- 若+1后仍是S.value<=0 表示依然有进程在等待该类资源 因此应调用wakeup原语唤醒等待队列中的第一个进程
	- 被唤醒进程从阻塞态->就绪态
### (2)信号量机制
#### ①实现进程互斥
- 分析并发进程的关键活动 划定临界区
- 设置互斥信号量mutex  初始值为1(可以理解为进入临界区的名额)
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/715b08f493c8329184e5d50b0871a456.png)
#### ②实现进程同步
要让各并发进程按要求有序的推进
- 分析什么地方需要实现“同步关系” 即必须保证“一前一后”执行的两个操作
- 设置同步信号量S 初始为0
- 在“前操作”之后执行V(S)
- 在“后操作”之前执行P(S)
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/251e7c10f704d4ed861bcfdfb8b28702.png)
#### ③实现进程的前驱关系
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/9029f40d703c3e96c51ce79f1368c2ab.png)
## 2.3.5 几种模型
### (1)生产者消费者问题
系统中有一组生产者和一组消费者进程，生产者进程每次生产一个产品放入缓冲区，消费者进程每次从缓冲区取出一个产品并使用
生产者、消费者共享一个初始为空、大小为n的缓冲区
- 只有缓冲区没满时，生产者才能把产品放入缓冲区，否则必须等待
- 只有缓冲区不空时，消费者才能从中取出产品，否则必须等待
- 缓冲区是临界资源，各进程必须互斥的访问
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/488007d66e627aa5a0864406f3ded05a.png)
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/4c2960beea033611b46c2bcb5b0f7174.png)
### (2)多生产者-多消费者问题
多种生产者和多种消费者
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/46e6760acdba0a3f306b8c204210fe5f.png)
### (3)读者-写者问题
- 读者和写者两组并发进程 共享一个文件 当两个或两个以上的读进程同时访问共享数据不会产生副作用 但是某个写进程和其他进程同时访问共享数据时则可能导致数据不一致的错误
- 要求
	- 允许多个读者可以同时对文件执行读操作
	- 只允许一个读者往文件中写信息
	- 任一写者在完成写操作之前不允许其他读者或写者工作
	- 写者执行写操作前 应让已有的读者和写者全部退出
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/75823c6bdf213032c7def608f3f8f224.png)
### (4)哲学家进餐问题
解决办法
- 施加限制，比如最多四位哲学家同时进餐，这样可以保证至少有一个哲学家可以拿到左右手的两只筷子
- 要求奇数号先拿左边筷子 偶数号先拿右边筷子
- 仅当一个哲学家左右两支筷子都可用才允许他抓起筷子
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/f4049e5b3ec6aee9efbc9cb28e72ce7c.png)
## 2.3.6 管程
>考试范围没有
## 2.4 死锁
### (1)什么是死锁
在并发环境下  各进程因竞争资源造成互相从对方手里获取资源，导致各个进程都阻塞无法向前推进的现象
### (2)死锁、饥饿、死循环的区别
- 死锁：各进程互相等待对方手里的资源，导致各进程都阻塞，无法向前推进的现象
- 饥饿：由于长期得不到想要的资源，某进程无法向前推进的现象
- 死循环：某进程执行过程中一直跳不出来某个循环的现象
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/91d9c2d8ba060fe58e1a3cea88dc4a11.png)
### (3)死锁产生的必要条件
- 互斥条件：只有对必须互斥使用的资源的争抢才会导致死锁
- 不剥夺条件：进程所获得的资源在未使用完前 不能由其他进程强行夺走 只能主动释放
- 请求和保持条件：进程已经保持了至少一个资源 但又提出了新的资源请求 而该资源又被其他进程占有 此时请求进程被阻塞 但又对自己的已有资源保持不变
- 循环等待条件：存在一种进程资源的循环等待链，链中的每一个进程已获得的资源同时被下一个进程所获得
### (4)什么时候会发生死锁
- 对不可剥夺资源的不合理分配
	- 对系统资源的竞争。各资源对不可剥夺的资源的竞争(如打印机)可能会导致死锁 对可剥夺的资源(CPU)的竞争是不会引起死锁的
	- 进程推荐顺序非法。请求和释放资源的顺序不当，也会导致死锁(哲学家进餐问题)
	- 信号量的使用不当也是造成死锁(生产者消费者的P操作问题)
### (5)死锁的处理策略
#### ①静态策略：预防死锁
- 破坏互斥条件
	- 互斥条件：只有对必须互斥使用的资源的争抢才会导致死锁
	- 通过SPOOLing技术可以将资源改造成可共享使用的资源
- 破坏不剥夺条件
	- 不剥夺条件：进程所获得的资源在未使用完之前 不能由其他进程强行夺走 只能主动释放
	- 方案一：当某个结合层请求新的资源得不到满足时 它必须立即释放保持的所有资源 待以后需要时再重新申请 也就是说 即使某些资源尚未使用完 也需要主动释放 从而破坏了不可剥夺条件
	- 方案二：当某个进程需要的资源被其他进程所占有的时候 可以由操作系统协助 将想要的资源强行剥夺 这种方式一般需要考虑各进程的优先级
- 破坏请求和保持条件
	- 请求和保持条件：进程已经保持了至少一个资源 但又提出了新的资源请求 而该资源又被其他进程占有 此时请求进程被阻塞 但又对自己有的资源保持不放
	- 可采用静态分配方法：进程在运行前一次申请完它所需要的全部资源 在它的资源未满足前 不让它投入运行 一旦投入运行后 这些资源就一直归他所有 该进程就不会请求别的任何资源了
- 破坏循环等待条件
	- 循环等待条件：存在一种进程资源的循环等待链，链中的每一个进程已获得的资源同时被下一个进程所请求
	- 可采用顺序资源分配法：首先给系统中的资源编号 规定每个进程必须按编号递增的顺序请求资源 同类资源一次申请完
#### ②动态策略：避免死锁
- 什么是安全序列
	- 指的是如果系统按照这种序列分配资源，则每个进程都能顺利完成。只要能找出一个安全序列，系统就是安全状态
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/e281b6388d89380359b3515308c3b5fa.png)
- 什么是系统的不安全状态？与死锁有什么联系？
	- 如果分配了资源之后 系统找不出任何一个安全序列 系统就进入了不安全状态
	- 如果系统处于安全状态就一定不会出现死锁 如果系统进入不安全状态 就可能出现死锁
- 如何避免系统进入不安全状态--银行家算法    题目2.4.3  10:00
	- ①检查此次申请是否超过了之前声明的最大需求数
	- ②检查此时系统剩余的可用资源是否还能满足这次请求
	- ③试探着分配 更改各数据的结构
	- ④用安全性算法检查此次分配是否会导致系统进入不安全状态
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/05843a1f3ad70295579c750edf2861da.png)
#### ③死锁的检测和解除
> 考纲没有
# 第3章  内存管理
## 3.1.1 基础知识
基本概念
## 3.1.2 管理方式和算法
### (1)连续分配管理
#### ①单一连续分配
- 只支持单道程序
- 无外部碎片，有内部碎片
	- 内部碎片：进程分到了内存但是没用到
	- 外部碎片：内存中的某些空闲分区由于太小而难以利用
  - 紧凑技术：把外部碎片拼凑在一起
#### ②固定分区分配
- 分区大小相等 & 分区大小不等
- 无外部碎片，有内部碎片
#### ③动态（可变）分区分配
- 空闲分区表 & 空闲分区链
  - 分区号 - 分区大小 - 起始地址 - 状态
- 无内部碎片，有外部碎片
### (2)动态分区分配算法
#### ①首次适应
从头到尾找到合适的分区
#### ②最佳适应
优先使用更小的分区，以保留更多大的分区
#### ③最坏适应
优先使用大的分区，防止产生太小的不可用碎片
#### ④临近适应
由首次适应演变，从上次查找结束的位置开始查找
## 3.1.3   基本分页管理
### 1.基本分页存储管理的概念
- 内存：页框号=页帧=内存块=物理块=物理页面
- 进程逻辑地址：页页面
- 页表：页号-块号(通常存在pcb)
	- 问题：页表算每个页表项的大小（3.1.3.1   5：00）
	- 问题：实现地址转换(3.1.3.1  9：13)  二进制无符号左移右移 #待看
### 2.基本地址变换机构
- 页表项长度：每个页表项大小占多大的存储空间(块号)
- 页表长度：有几个页表项？
- 页面大小：一个页面占多大的存储空间
- 偏移量占十位  那么说明页面大小是十位
### 3.具有快表的地址变换机构
- 快表(TLB)：存放最近访问的页表项的副本 加速地址变换的速度
- 慢表：内存中的页表
- 访问时候先判断是否越界异常  再查询TLB 没有命中再去查询页表
- 访问
	- 在TLB找到访问的页号 直接读取拼接内存快号和偏移量 访问物理地址对应的内存单元----访问一次
	- 没有找到访问的页号 先访问内存中的页表 找到页表项拼接  再访问物理地址对应的内存单元 ----访问两次
		- 找到页表项后应该同时存入快表 满的按照一定算法对旧的进行替换 
	- 题目：地址变换过程  3.1.3.3    13：48
	- 时间局部性&空间局部性
		- 时间：一个指令可能被再次使用
		- 空间：访问过后 临近的被再次访问
### 4.两级页表
- 页目录表
	- 页号位数+块号位数=逻辑地址空间位数
	- 地址变换：一级页号-二级页号-页内偏移量
## 3.1.4   分段存储
- 每一段的地址从0开始
- 段号-段内偏移量
- 段号：计算共有多少个段
- 段内地址：计算段的最大长度
- 段表：段号-段长-基址
	- ①先比较题中段号和段表长度
	- ②计算段号对应的段表项
	- ③对比段内地址和段长
## 3.1.5  段页式存储
 - 段号的位数：决定每个进程最多可以分为几个段
 - 页号位数：决定每个段最大有多少页
 - 页内偏移量：决定了页面大小、内存块大小是多少
 - 段表项结构：段号-页表长度-页表存放块号
	 - 先根据逻辑地址得到 段号-页号-页内偏移量
	 - 再判断段号是否越界 
	 - 查询段表：段号-页表长度-页表存放块号
	 - 检查页号是否越界
	 - 计算 拼接
- 三次访存
	- 先访问段表
	- 再访问页表
	- 最后访问内存目标单元
## 3.2.1  虚拟存储
- 请求分页存储管理
- 请求分段存储管理
- 请求段页式存储管理
## 3.2.2  请求分页管理方式
- 页表
	- 内存块号
	- 状态位：0/1  是否被调入内存 0为否
	- 访问字段：可记录最近被访问的时间 供置换算法选择换出页面时参考
	- 修改位：页面调入内存后是否被修改过
	- 外存地址
## 3.2.4  页面置换算法
### (1)最佳置换算法 OPT
每次选择淘汰的页面将是最长时间不使用的

| 访问页面 | 7   | 0   | 1   | 2   | 0   | 3   | 0   | 4   | 2   | 3   | 0   | 3   | 2   | 1   | 2   | 0   | 1   | 7   | 0   | 1   |
| ---- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 内存块1 | 7   | 7   | 7   | 2   |     | 2   |     | 2   |     |     | 2   |     |     | 2   |     |     |     | 7   |     |     |
| 内存块2 |     | 0   | 0   | 0   |     | 0   |     | 4   |     |     | 0   |     |     | 0   |     |     |     | 0   |     |     |
| 内存块3 |     |     | 1   | 1   |     | 3   |     | 3   |     |     | 3   |     |     | 1   |     |     |     | 1   |     |     |

### (2)先进先出置换算法 FIFO
- 每次淘汰的页面时最早进入内存的页面
- 内存块多了 可能会导致缺页率变大 这是贝拉异常
### (3)最近最久未使用置换算法 LRU

| 访问页面 | 1   | 8   | 1   | 1   | 7   | 8   | 2   | 7   | 2   | 1   | 8   | 3   | 8   | 2   | 1   | 3   | 1   | 7   | 1   | 3   | 7   |
| ---- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 内存块1 | 1   | 1   |     |     | 1   |     | 1   |     |     |     |     | 1   |     |     |     |     |     | 1   |     |     |     |
| 内存块2 |     | 8   |     |     | 8   |     | 8   |     |     |     |     | 8   |     |     |     |     |     | 7   |     |     |     |
| 内存块3 |     |     |     |     | 7   |     | 7   |     |     |     |     | 3   |     |     |     |     |     | 3   |     |     |     |
| 内存块4 |     |     |     |     |     |     | 2   |     |     |     |     | 2   |     |     |     |     |     | 2   |     |     |     |

### (4)时钟置换算法 CLOCK(最近未用算法)
- 链接指正的方式 先围成一个圈  被访问过为1  最近没访问过为0
- 从队首开始扫描    访问下一个
	- 如果是1 则变成0 且扫描下一个
	- 如果是0 则变成1 替换当前的
### (5)改进型的时钟置换算法
- 算法大体相似 但是如果当前的可能要被淘汰的页面没有修改 则不需要返回内存 则可以替换
	- 第一轮扫描：从第一个开始扫描(0,0)用于替换 不修改任何的标志位
	- 第二轮扫描：若第一轮失败 则重新扫描  查找第一个(0,1) 用于替换 且扫描过的访问位变为0
	- 第三轮扫描：若第二轮失败 则重新扫描  查找第一个(0,0)用于替换 不修改任何的标志位
	- 第四轮扫描：若第三轮失败 则重新扫描  查找第一个(0,1)用于替换  一定能找到
- 最多四轮扫描
## 3.2.3 + 3.2.5 页面分配策略

## 3.2.7 内存映射文件

# 第4章  文件管理
## 4.1.1 文件系统概念
### (1)文件的属性
- 文件名：由创建文件的用户决定文件名 同一目录下不允许有重名文件
- 标识符：一个系统内个文件标识符唯一 只是操作系统用于区分各个文件的一种内部名称
- 类型：文件的类型 比如txt mp4等等
- 位置：用户的存放路径 在外存的存放地址
- 大小：文件占用空间大小
- 创建时间、上次修改时间
- 文件所有者信息
- 保护信息：对文件进行保护的访问控制信息
### (2)组织方式
- 无结构文件(流式文件)：由一些二进制或字符流组成 如文本文件
- 有结构文件(记录式文件)：由一组相似的记录组成 如数据库表
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/cef2ed37b4035375f65eae278d99be6f.png)
### (3)os向上提供的功能
- 最基本的功能
	- 打开文件(open 系统调用)
	- 创建文件(create 系统调用)
	- 删除文件(delete 系统调用)
	- 读文件(read 系统调用)
	- 写文件(write 系统调用)
	- 关闭文件(close 系统调用)
- 实现逻辑
	- 可以创建文件 点击新建后 图形化交互过程在背后调用了“create系统调用”
	- 可以读文件 将文件数据读入内存 才能让CPU处理 调用了os的读文件功能 即“read系统调用”  将文件数据从外存中读入内存 并显示
	- 可以写文件 将更改后的文件数据写回外存 点击保存后 调用了os的写文件功能 即“write系统调用” 将文件数据从内存写回外存
## 4.1.2 文件的逻辑结构
### (1)无结构文件
流式文件 由一些二进制或字符流组成 如文本文件
### (2)有结构文件
记录式文件 由一组相似的记录组成 如数据库表
一般来说 每条记录有一个数据项可作为关键字
根据各条记录的长度(占用的存储空间)是否相等 又可分为定长记录和可变长记录
#### ①顺序文件
文件中的记录一个接着一个地顺序排列(逻辑上) 记录可以是定长的或可变长的
各个记录在物理上可以顺序存储或链式存储
- 分类
	- 顺序结构：记录之间的顺序按关键字顺序排列
		- 可变长记录：无法实现随机存储 每次只能从第一个记录开始依次往后查找
		- 定长记录
			- 可实现随机存储 记录长度为L  则第i个记录存放的相对位置是i × l
			- 若采用串结构 无法快速找到某关键字对应的记录
			- 若采用顺序结构 可以快速找到某关键字对应的记录(如折半查找)
	- 串结构：记录之间的顺序与关键字无关
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/9313153deefc5eb1093c6fbf51aa80a7.png)
#### ②索引文件
本身是定长记录的顺序文件 因此可以快速找到第i个记录对应的索引项
可以将关键字作为索引号内容 若按关键字顺序排列 则还可以支持按照关键字折半查找
每当要增加/删除一个记录时 需要对索引表进行修改
由于索引文件有很快的检索速度 因此主要对信息处理的及时性要求比较高的场合
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/fd6c4c19c4de585a71c1cf9dad9f3d80.png)
#### ③索引顺序文件
本身是索引文件和顺序文件思想的结合
索引顺序文件中 同样会为文件建立一张索引表
但并不是每个记录对于一个索引表项 而是一组记录对于一个索引表项
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/6158c89e261901a69834fb90f642e1a0.png)
#### ④多级索引顺序文件
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/091c69304b22a68feb3cefa47f73d529.png)
## 4.1.3 文件目录 FCB
### (1)文件控制块 
目录本身就是一种有结构文件 由一条条记录组成
每条记录对应一个放在该目录下的文件
`|文件名 | 类型 | 存取权限 | ..... | 物理地址|`
### (2)目录结构
#### ①单级目录结构
早期的os不支持多级目录 整个系统直建立一张目录表 每个文件占一个目录项 单级文件不允许重名
#### ②两级目录结构
分为主文件目录(MFD,Master File Directory)和用户文件目录(UFD,User File Directory)
#### ③多级目录结构(树形目录结构)
用户要访问某个文件时要用文件路径名标识文件 文件路径名是个字符串
各级目录之间用"/"隔开
从根目录出发的路径称为绝对路径
从当前目录出发的路径称为相对路径
#### ④无环目录结构
在属性目录结构的基础上 增加一些指向同一节点的有向边 使整个目录成为一个有向无环图 实现多用户间的文件共享
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/5c49a9395b1b28cdaaf714590ded49d1.png)
### (3)索引节点
>考纲没有

## 4.1.4 文件的物理结构
### (1)对非空闲磁盘块的管理
很多os中 磁盘块的大小和内存块、页面的大小相同
每次读写操作都是以块为单位进行的
逻辑地址也可以表示为(逻辑块号，块内地址)的形式
#### ①连续分配
每个文件在磁盘上占有一组连续的块
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/ab13902e70a0baf71af80f822bf8b9d7.png)
物理块号=起始块号+逻辑块号
可以直接逻辑块号对应的物理块号 一次连续分配支持顺序访问和直接访问
- 优点：方便访问 效率高
- 缺点：不方便文件拓展 无法充分利用空闲块 会产生碎片
#### ②链接分配
 - 隐式链接
- 如果访问i号块 先找到FCB 从逻辑0号块依次读取 共i+1次
	- 优点：很方便支持文件拓展 不会有碎片问题 外存利用率高
	- 缺点：只支持顺序访问 查找效率低 指向下一个盘块的指针也消耗少量的存储空间
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/7affa4ab4ef5ccc5fbcec19a4d705a7d.png)
- 显示链接
	- 优点：很方便文件拓展 不会有碎片 外存利用率高 支持随机访问
	- 缺点：文件分配表占据一定的存储空间
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/9f33314b02fb587011361b3ea5afe91e.png)
#### ③索引分配
索引表记录了文件的各个逻辑块对应的物理块
索引表存放的磁盘块成为索引块
文件数据存放的磁盘块称为数据块
可以使用固定长度表示物理块号  因此逻辑块号是可以隐藏的
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/2fccc6f085c93aed3297b7b1b5531f1d.png)
# 第5章 设备管理
## 5.3.1磁盘的结构
### (1)磁盘、磁道、扇区的概念
#### ①磁盘
- 磁盘的由一些磁性物质组成，可以用这些磁性物质来记录二进制数据
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/96ab18af555f899b74545f8c7fa194b6.png)
#### ②磁道
- 磁盘的盘面被划分为一个个磁道，这样的一个个"圈"就是一个磁道
#### ③扇区
- 一个磁道又被划分成一个个扇区，每个扇区就是一个“磁盘块”
- 各个扇区存放的数据量相同  
- 由于往内面积越小 但是数据量相同 因此越往内数据密度越大
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/f9bcd89f33482df3a6a808bc19f2e3f7.png)
### (2)如何在磁盘中读/写数据
- 需要把“磁头”移动到想要读写的扇区所在的磁道
- 磁盘会转起来 让目标磁头从下面划过 才能完成读写操作
### (3)盘面、柱面的概念
![](https://raw.githubusercontent.com/Rainan249/my-blog/main/content/posts/images/8cb8f5ed24123e6c733af8e64c71dcd2.png)
### (4)磁盘的物理地址
- 可以用(柱面号，盘面号，扇区号)来定义任何一个“磁盘块”
- 在文件的物理结构中 文件数据放在外存中的几号块 这个块号就可以写成上面的地址形式
### (5)磁盘的分类
磁头是否可以获得技能
- 活动头磁盘(左图)：磁头可以活动的磁盘，磁臂可以来回伸缩来带动磁头定位磁道
- 固定头磁盘(右图)：磁头不可移动的磁盘 ，但是每个磁道都有一个磁头
![](attachment/453d517a14c062b5520235e83d7e3e86.png)
- 按照底盘是否可以分类
	- 可换盘磁盘：盘片可以更换
	- 固定盘磁盘：盘片不可以更换
## 5.3.2磁盘调度算法
### (1)一次磁盘读/写操作需要的时间
#### ①寻找时间
Ts：在读写数据前 将磁头移动到指定磁道所花的时间
- 启动磁头臂式需要时间的 假设耗时为s
- 移动磁头也是需要时间的 假设磁头匀速移动 每跨越一个磁道耗时为m 总共需要跨越n条磁  则耗时为m×n
	- 则寻道时间为Ts = s + m × n
#### ②延迟时间
Tr：通过旋转磁盘 使磁头定位到目标扇区所需要的时间
- 设转速为r 
	- 则平均所需延迟时间为 Tr = (1/2)×(1/r) = 1/2r
#### ③传输时间
Tt：从磁盘读出或向磁盘写入数据所经历的时间 
- 假设磁盘转速为r 此次读写的字节数为b 每个磁道上的字节数为N
	- 则传输时间Tt = (1/r)×(b/N)=b/(rN)
### (2)磁盘调度算法
tips：平均寻找长度 = 磁头总移动磁道数 ÷ 访问磁道数量
#### ①先来先服务 FCFS
根据进程请求访问磁盘的先后顺序进行调度
- 优点：公平 如果请求访问的磁道比较集中的话 算法性能还行
- 缺点：如果有大量的进程竞争使用磁盘 请求访问的磁道很分散 则算法性能很差 寻道时间长
#### ②最短寻找时间优先 SSTF
- 优先处理的磁道是与当前磁头最近的磁道 
- 可以保证每次的寻道时间最短 但是并不能保证总的寻道时间最短
- 本质是贪心算法的思想 只选择眼前最优的 但是总体未必最优
- 优点：性能较好 平均寻道时间短
- 缺点：可能产生饥饿现象
	- 如果有源源不断的临近磁道访问请求 则距离远的磁道会产生饥饿现象
#### ③扫描算法(电梯算法) SCAN
只有磁头移动到最外侧的时候才能往内移动 移动到最内侧磁道时候才能往外移动
- 优点：性能较好 平均寻道时间较短 不会产生饥饿现象
- 缺点：对各个磁道的响应频率不平均
- tips：LOOK调度算法 在SCAN基础上改进 如果在磁头移动方向没有别的请求 就可以立即改变磁头移动方向
#### ④循环扫描算法 C-SCAN
只有磁头朝某个特定方向移动时才处理磁道访问请求，而返回时直接快速移动至起始端而不处理任何请求
- 优点：对每个磁道的相应频率很平均
- 缺点：平均寻道时间更长
- tips：C-LOOK调度算法 在C-SCAN基础上改进 如果在磁头移动方向没有别的请求 就可以立即改变磁头移动方向